#%RAML 1.0 DataType

usage: |
  It should be used whenever OAuth parameters are required for either query
  parameters or the body.

description: |
  A list of cookies. Keys are cookies `name`s and values are cookies
  `value`s.
type: object

properties:
  /^[a-zA-Z0-9\-_\.]+$/:
    type: string
    description: Any cookie name.
